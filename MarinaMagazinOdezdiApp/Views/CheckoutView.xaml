<UserControl x:Class="MarinaMagazinOdezdiApp.Views.CheckoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
        <StackPanel MinWidth="500">
            <TextBlock Text="Оформление заказа" Style="{StaticResource TextBlock.Title}" HorizontalAlignment="Center"/>

            <GroupBox Header="Адрес доставки" Margin="0,10,0,20">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Город:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Column="1" Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>

                    <TextBlock Grid.Row="1" Text="Улица:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>

                    <TextBlock Grid.Row="2" Text="Дом/Квартира:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding HouseNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Способ оплаты" Margin="0,5,0,20">
                <StackPanel Margin="10">
                    <RadioButton Content="Картой при получении" IsChecked="{Binding IsCardPayment}" GroupName="Payment"/>
                    <RadioButton Content="Наличными при получении" IsChecked="{Binding IsCashPayment}" GroupName="Payment" Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding TotalPrice, StringFormat='Итого к оплате: {0:C}', ConverterCulture=ru-RU}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Подтвердить заказ" Command="{Binding PlaceOrderCommand}" Margin="20,0,0,0"/>
            </Grid>

            <Button Content="Отмена" Command="{Binding CancelCommand}" HorizontalAlignment="Center" Margin="0,30,0,0"/>

        </StackPanel>
    </Grid>
</UserControl>