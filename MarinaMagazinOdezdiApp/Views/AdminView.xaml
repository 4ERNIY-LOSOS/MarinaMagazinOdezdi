<UserControl x:Class="MarinaMagazinOdezdiApp.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:MarinaMagazinOdezdiApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Товары">
                <views:ProductManagementView DataContext="{Binding ProductManagementVM}"/>
            </TabItem>
            <TabItem Header="Категории">
                <views:CategoryManagementView DataContext="{Binding CategoryManagementVM}"/>
            </TabItem>
            <TabItem Header="Пользователи">
                <views:UserManagementView DataContext="{Binding UserManagementVM}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>