<Window x:Class="MarinaMagazinOdezdiApp.Views.AddEditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Grid Margin="20" MinWidth="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Email:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding User.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Имя:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding User.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Фамилия:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding User.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Роль:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}" Margin="0,10,0,10"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Command="{Binding SaveCommand}" IsDefault="True" MinWidth="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" IsCancel="True" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>